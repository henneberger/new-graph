--- metadata
{"dataset":"CSV tck","expected_kind":"rows","id":"cypher.ladybug.tck.expressions.precedence.Precedence1.Scenario17.e0272b3e6a28","language":"cypher","ordered":true,"source":"tck/expressions/precedence/Precedence1.test","source_case":"Scenario17","source_log":null,"status":"imported","suite":"ladybug","version":1}
--- query
UNWIND [True, False, null] AS a UNWIND [True, False, null] AS b WITH collect((NOT a AND b) = ((NOT a) AND b)) AS eq, collect((NOT a AND b) <> (NOT (a AND b))) AS neq RETURN all(x IN eq WHERE x) AND any(x IN neq WHERE x) AS result
--- expected
True
